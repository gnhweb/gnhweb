# 늑대와 양 게임 — 성능 측정 기준선 (Baseline)

> 1단계에서 추가한 진단 코드(`perfDebug.ts`, `MainScene.ts`의 `[1단계 진단용]` 표시 구간)로
> 측정한 결과를 이 문서에 채워넣으세요. 2단계, 3단계 작업 후에도 같은 방식으로 측정해서
> 표에 열을 추가하면 개선 효과를 숫자로 비교할 수 있습니다.

## 측정 방법
1. `npm run dev`로 로컬 서버 실행
2. 모바일 기기 브라우저(또는 PC Chrome의 개발자도구 → Device Toolbar → 성능 탭에서
   CPU 4x~6x throttling 설정)로 게임 접속 → 로비에서 게임 시작
3. 화면 좌상단에 뜨는 초록색 오버레이에서 FPS를 확인 (0.2초마다 갱신됨)
4. 브라우저 콘솔(F12 → Console)에서 `[perfDebug] create() 직후 ...` 로그와
   오브젝트 개수 표(`console.table`)를 캡처
5. 안개 영향을 보려면 주소창에 `?nofog=1`을 붙여서 재접속 (예: `http://localhost:5173/?nofog=1`)
   → 같은 상황에서 FPS를 다시 확인

## 1단계 측정 결과 (여기를 채우세요)

### 오브젝트/마스크 개수 (콘솔 로그에서 복사)
- 총 GameObject 개수: `____`개
- 마스크(mask) 걸린 오브젝트 개수: `____`개
- 종류별 상위 항목 (console.table 캡처 붙여넣기):
```
(여기에 콘솔 표 붙여넣기)
```

### FPS 측정
| 측정 시나리오 | 기기/환경 | FPS |
|---|---|---|
| 로비 화면 | | |
| 게임 시작 직후 정지 상태 | | |
| 이동 중 (평지) | | |
| 이동 중 (안개 있는 상태) | | |
| 이동 중 + `?nofog=1` (안개 없는 상태) | | |

### 안개 영향 요약
- 안개 ON일 때 FPS: `____`
- 안개 OFF(`?nofog=1`)일 때 FPS: `____`
- 차이: `____` (이 차이가 크면 2단계에서 안개 렌더링 교체를 최우선으로 진행)

### Chrome DevTools Performance 탭 요약 (선택, 가능하면 진행)
- 3~5초 녹화 시 "Rendering" 비율: `____`%
- "GPU" 비율: `____`%
- 스크린샷 첨부 위치: `____`

## 2단계 이후 (2단계 작업 완료 후 여기에 추가)

## 3단계 이후 / 최종 (3단계 작업 완료 후 여기에 추가)

### 측정 방법 (1단계와 동일 + 저사양 모드 테스트 추가)
1. `npm run dev` → 모바일 기기 또는 DevTools 스로틀링으로 접속
2. 콘솔에서 `[perf] 초기 성능 티어: ...` 로그 확인 (기기 스펙 기반 1차 판정 결과)
3. 5초 이상 플레이하며 FPS가 40 밑으로 떨어지면 `[perf] 실측 FPS 저조 ... 저사양 모드로 전환` 경고가 뜨는지 확인 (2차 강등이 실제로 동작하는지)
4. 저사양 분기 로직 자체를 강제로 테스트하려면 주소에 `?lowspec=1`(강제 저사양) 또는 `?highspec=1`(강제 고사양)을 붙여서 재접속 — 기기 스펙과 무관하게 지정한 티어로 시작한다
5. 8인 기준으로 다른 플레이어들을 화면 밖으로 나가게 이동한 뒤(카메라 뷰포트 밖) FPS가 더 오르는지 확인 (화면 밖 캐릭터 컬링 효과 확인용)

### FPS 측정 (최종)
| 측정 시나리오 | 기기/환경 | 1단계 | 2단계 | 3단계(고사양) | 3단계(`?lowspec=1`) |
|---|---|---|---|---|---|
| 로비 화면 | | | | | |
| 게임 시작 직후 정지 상태 | | | | | |
| 이동 중 (다른 플레이어 화면 안) | | | | | |
| 이동 중 (다른 플레이어 대부분 화면 밖) | | | | | |
| 킬 이펙트 발생 순간 | | | | | |

### 1단계 대비 개선율
- 로비: `____`%
- 이동 중: `____`%
- 개선이 크게 부족하다면: (a) 실기기가 정말 저사양 브랜치를 타는지 콘솔 로그로 재확인, (b) Chrome DevTools Performance 탭으로 재프로파일링해서 남은 병목이 렌더링/GPU가 맞는지, 혹시 네트워크(Supabase Realtime) 쪽 지연이 "렉"으로 오인되고 있는 건 아닌지 확인

### 실기기 플레이 테스트 요약
- 기기: `____`
- 결과: `____`