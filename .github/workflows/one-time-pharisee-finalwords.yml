name: One-time Pharisee Final Words Vote Migration
"on":
  push:
    branches:
      - main
permissions:
  contents: write
jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify workflow execution
        run: echo "pharisee final words migration runner active"
