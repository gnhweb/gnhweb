name: Playwright Mobile E2E

on:
  push:
    branches: ['**']
  pull_request:
  workflow_dispatch:

env:
  NODE_ENV: development

jobs:
  mobile-e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      E2E_BASE_URL: https://gnhweb-git-${{ github.head_ref || github.ref_name }}-gnhweb1.vercel.app
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install dependencies
        run: npm install --include=dev --cache "/tmp/npm-cache-${{ github.run_id }}"
      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium webkit
      - name: Wait for Vercel preview
        run: |
          for attempt in {1..30}; do
            status=$(curl -L -s -o /dev/null -w '%{http_code}' "$E2E_BASE_URL")
            if [ "$status" -ge 200 ] && [ "$status" -lt 500 ]; then exit 0; fi
            sleep 10
          done
          echo "Vercel preview did not become reachable: $E2E_BASE_URL"
          exit 1
      - name: Run mobile E2E
        run: npm run test:e2e
      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          if-no-files-found: ignore
