# Disabled legacy repair workflow. Kept inert so it cannot modify the repository automatically.
name: Disabled legacy Bible Pick repair
on:
  workflow_dispatch:
permissions: {}
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy repair workflow is disabled."
