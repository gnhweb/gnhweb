import { useState, useRef, useEffect } from 'react';
import { Link, useNavigate, useLocation } from 'react-router-dom';
import { motion, AnimatePresence } from 'framer-motion';
import { useAuth } from '@/hooks/useAuth';
import { useTheme } from '@/hooks/useTheme';
import { useMobileMenu } from '@/hooks/useMobileMenu';
import { ROLE_LABELS, CLUB_LABELS } from '@/types/auth';
import type { UserRole } from '@/types/auth';
import MeetingIdeasModal from '@/components/feature/MeetingIdeasModal';
import NotificationsModal, { useNotificationCount, NotificationToast } from '@/components/feature/NotificationsModal';

const MotionLink = motion(Link);

const TOP_ITEMS: { path: string; label: string; icon: string }[] = [
  { path: '/', label: '홈', icon: 'ri-home-line' },
  { path: '/clubs', label: '동아리', icon: 'ri-group-line' },
  { path: '/notices', label: '공지사항', icon: 'ri-megaphone-line' },
  { path: '/schedule', label: '일정', icon: 'ri-calendar-event-line' },
  { path: '/suggestions', label: '건의사항', icon: 'ri-lightbulb-line' },
  { path: '/qna-board', label: '질문 있어요', icon: 'ri-question-answer-line' },
];

interface CategoryItem { path: string; label: string; icon: string; }
interface CategoryGroup { name: string; icon: string; colorClass: string; items: CategoryItem[]; }

const BIBLE_CATEGORY: CategoryGroup = { name: '말씀 도구', icon: 'ri-book-open-line', colorClass: 'amber', items: [{ path: '/bible-pick', label: '말씀뽑기', icon: 'ri-book-open-line' }, { path: '/bible-quiz', label: '성경 퀴즈', icon: 'ri-question-answer-line' }, { path: '/bible-mbti', label: '말씀 MBTI', icon: 'ri-user-heart-line' }, { path: '/bible-by-age', label: '연령별 말씀', icon: 'ri-book-read-line' }, { path: '/bible-marathon', label: '성경 완독', icon: 'ri-book-open-line' }] };
const COMMUNITY_CATEGORY: CategoryGroup = { name: '소통·공동체', icon: 'ri-group-line', colorClass: 'emerald', items: [{ path: '/memory-board', label: '추억창', icon: 'ri-image-line' }, { path: '/song-vote', label: '찬양투표', icon: 'ri-music-line' }, { path: '/prayer-partner', label: '신앙 짝꿍', icon: 'ri-heart-pulse-line' }, { path: '/prayer-relay', label: '기도 릴레이', icon: 'ri-hand-heart-line' }, { path: '/missions/wall', label: '사명 인증 게시판', icon: 'ri-gallery-line' }] };
const GAME_CATEGORY: CategoryGroup = { name: '갓겜', icon: 'ri-gamepad-line', colorClass: 'indigo', items: [{ path: '/games', label: '전체 게임 보기', icon: 'ri-apps-2-line' }, { path: '/pharisee', label: '바리새인을 찾아라', icon: 'ri-book-open-line' }, { path: '/wolves-and-sheep', label: '양과 늑대', icon: 'ri-user-3-line' }, { path: '/galilee-phone', label: '갈릴리폰', icon: 'ri-chat-smile-3-line' }] };
interface MissionSubSection { label: string; items: { path?: string; label: string; icon: string; action?: string }[]; }
const MISSION_SUBSECTIONS: MissionSubSection[] = [
  { label: '출석 관리', items: [{ path: '/attendance-board', label: '실시간 출석 현황판', icon: 'ri-user-heart-line' }] },
  { label: '회의록', items: [{ path: '/meetings', label: '회의록', icon: 'ri-chat-check-line' }, { path: '/meeting-copilot', label: '회의 코파일럿 AI', icon: 'ri-lightbulb-flash-line' }] },
  { label: '보고서', items: [{ path: '/reports/weekly', label: '주간 보고서', icon: 'ri-file-list-3-line' }, { path: '/reports/growth', label: '성장 기록', icon: 'ri-plant-line' }, { path: '/reports/events', label: '행사 보고서', icon: 'ri-calendar-event-line' }, { path: '/visitations', label: '심방 스케줄', icon: 'ri-heart-pulse-line' }] },
  { label: '사명 도구', items: [{ path: '/pds-planner', label: '행사 기획 마법사', icon: 'ri-todo-line' }, { path: '/event-ideas', label: '행사 기획 아이디어', icon: 'ri-lightbulb-flash-line' }, { path: '/leadership-diary', label: '리더십 코칭', icon: 'ri-book-read-line' }] },
  { label: '미션', items: [{ path: '/missions', label: '작은 사명 관리', icon: 'ri-medal-line' }, { path: '/missions/leaderboard', label: '이달의 사명왕', icon: 'ri-trophy-line' }] },
];
interface AdminItem { label: string; icon: string; path: string; minRole: UserRole; }
const ADMIN_CATEGORY_ITEMS: AdminItem[] = [{ label: '가입 승인', icon: 'ri-user-star-line', path: '/admin/approvals', minRole: 'teacher' }, { label: '보고서 검토', icon: 'ri-file-search-line', path: '/reports/review', minRole: 'president' }, { label: '권한 관리', icon: 'ri-shield-keyhole-line', path: '/admin/roles', minRole: 'chief' }, { label: '전략 대시보드', icon: 'ri-bar-chart-line', path: '/admin/strategy', minRole: 'chief' }, { label: '불참 사유 설정', icon: 'ri-settings-3-line', path: '/settings/absence-reasons', minRole: 'chief' }, { label: '출석 위치 설정', icon: 'ri-map-pin-line', path: '/settings/attendance-location', minRole: 'teacher' }];
const PROFILE_FAITH_ITEMS: CategoryItem[] = [{ path: '/bible-pick/history', label: '말씀 히스토리', icon: 'ri-history-line' }, { path: '/bible-streak', label: '말씀 스트릭', icon: 'ri-fire-line' }, { path: '/faith-storybook', label: '신앙 스토리북', icon: 'ri-bookmark-line' }, { path: '/faith-journal', label: '신앙 일지', icon: 'ri-edit-line' }, { path: '/repentance-journal', label: '회개 저널', icon: 'ri-hand-heart-line' }, { path: '/bucket-list', label: '버킷리스트', icon: 'ri-todo-line' }, { path: '/year-end-summary', label: '월별 결산', icon: 'ri-calendar-check-line' }];
const FAITH_CATEGORY: CategoryGroup = { name: '신앙', icon: 'ri-heart-2-line', colorClass: 'primary', items: PROFILE_FAITH_ITEMS };
const PROFILE_ACTIVITY_ITEMS: CategoryItem[] = [{ path: '/personal-schedule', label: '개인 일정', icon: 'ri-calendar-check-line' }, { path: '/dashboard/attendance', label: '스마트 출석', icon: 'ri-user-heart-line' }, { path: '/missions/board', label: '작은 사명', icon: 'ri-medal-line' }];
function catBgActive(cat: string) { switch (cat) { case 'amber': return 'bg-amber-100 text-amber-700'; case 'emerald': return 'bg-emerald-100 text-emerald-700'; case 'rose': return 'bg-accent-100 text-accent-700'; case 'slate': return 'bg-secondary-100 text-secondary-700'; case 'primary': return 'bg-primary-100 text-primary-700'; case 'indigo': return 'bg-indigo-100 text-indigo-700'; default: return 'bg-primary-100 text-primary-700'; } }
function catBgHover(cat: string) { switch (cat) { case 'amber': return 'hover:bg-amber-50 hover:text-amber-700'; case 'emerald': return 'hover:bg-emerald-50 hover:text-emerald-700'; case 'rose': return 'hover:bg-accent-50 hover:text-accent-700'; case 'slate': return 'hover:bg-secondary-50 hover:text-secondary-700'; case 'primary': return 'hover:bg-primary-50 hover:text-primary-700'; case 'indigo': return 'hover:bg-indigo-50 hover:text-indigo-700'; default: return 'hover:bg-background-100 hover:text-foreground-950'; } }
function roleEmoji(role: UserRole) { switch (role) { case 'chief': return '👑'; case 'teacher': return '🍎'; case 'president': return '🎖️'; default: return '🌱'; } }
function ThemeToggleButton({ className = '' }: { className?: string }) { const { theme, toggleTheme } = useTheme(); const isDark = theme === 'dark'; return <button type="button" onClick={toggleTheme} aria-label={isDark ? '화이트 모드로 전환' : '다크 모드로 전환'} title={isDark ? '화이트 모드로 전환' : '다크 모드로 전환'} className={`flex items-center justify-center w-9 h-9 rounded-full bg-background-100 hover:bg-background-200 border border-background-200 text-foreground-600 hover:text-foreground-900 transition-all duration-200 cursor-pointer hover:scale-[1.05] ${className}`}><i className={`text-base ${isDark ? 'ri-sun-line' : 'ri-moon-line'}`}></i></button>; }
export default function Navbar() {
  const { user, profile, signOut, hasRole } = useAuth(); const navigate = useNavigate(); const location = useLocation();
  const [profileOpen, setProfileOpen] = useState(false), [bibleOpen, setBibleOpen] = useState(false), [faithOpen, setFaithOpen] = useState(false), [commOpen, setCommOpen] = useState(false), [missionOpen, setMissionOpen] = useState(false), [adminOpen, setAdminOpen] = useState(false), [gameOpen, setGameOpen] = useState(false);
  const { mobileOpen, setMobileOpen } = useMobileMenu(); const [mobileAccordion, setMobileAccordion] = useState<Record<string, boolean>>({}); const [meetingIdeasOpen, setMeetingIdeasOpen] = useState(false); const [notificationsOpen, setNotificationsOpen] = useState(false); const [globalSearch, setGlobalSearch] = useState(''); const [scrolled, setScrolled] = useState(false); const notificationCount = useNotificationCount(user);
  const profileRef = useRef<HTMLDivElement>(null), bibleRef = useRef<HTMLDivElement>(null), faithRef = useRef<HTMLDivElement>(null), commRef = useRef<HTMLDivElement>(null), missionRef = useRef<HTMLDivElement>(null), adminRef = useRef<HTMLDivElement>(null), gameRef = useRef<HTMLDivElement>(null);
  useEffect(() => { const onScroll = () => setScrolled(window.scrollY > 10); window.addEventListener('scroll', onScroll); return () => window.removeEventListener('scroll', onScroll); }, []);
  useEffect(() => { const refs = [profileRef, bibleRef, faithRef, commRef, missionRef, adminRef, gameRef]; const onClickOutside = (e: MouseEvent) => { if (refs.every(r => !r.current?.contains(e.target as Node))) { setProfileOpen(false); setBibleOpen(false); setFaithOpen(false); setCommOpen(false); setMissionOpen(false); setAdminOpen(false); setGameOpen(false); } }; document.addEventListener('mousedown', onClickOutside); return () => document.removeEventListener('mousedown', onClickOutside); }, []);
  useEffect(() => { setMobileOpen(false); setBibleOpen(false); setFaithOpen(false); setCommOpen(false); setMissionOpen(false); setAdminOpen(false); setGameOpen(false); setProfileOpen(false); setMobileAccordion({}); setGlobalSearch(''); }, [location.pathname]);
  const closeAllDesktop = () => { setBibleOpen(false); setFaithOpen(false); setCommOpen(false); setMissionOpen(false); setAdminOpen(false); };
  const isActive = (path: string) => location.pathname === path;
  const handleMissionAction = (item: { path?: string; action?: string }) => { closeAllDesktop(); setMobileOpen(false); if (item.path) navigate(item.path); else if (item.action === 'meeting-ideas') setMeetingIdeasOpen(true); };
  const handleSuggestions = (e: React.MouseEvent) => { e.preventDefault(); navigate('/suggestions'); };
  const handleSignOut = async () => { await signOut(); setProfileOpen(false); setMobileOpen(false); };
  const toggleMobileAccordion = (key: string) => setMobileAccordion(prev => ({ ...prev, [key]: !prev[key] }));
  const handleMobileBack = () => { if (window.history.length > 1) window.history.back(); else if (location.pathname !== '/') navigate('/'); };
  const isIosPwa = () => { if (typeof window === 'undefined' || typeof navigator === 'undefined') return false; const standalone = (navigator as Navigator & { standalone?: boolean }).standalone === true; const displayModeStandalone = window.matchMedia?.('(display-mode: standalone)').matches === true; return /iPhone|iPad|iPod/.test(navigator.userAgent) && (standalone || displayModeStandalone); };
  useEffect(() => { if (typeof window === 'undefined' || typeof document === 'undefined') return; if (!mobileOpen) return; const scrollY = window.scrollY; const body = document.body; body.dataset.scrollLockY = String(scrollY); body.style.top = `-${scrollY}px`; body.classList.add('scroll-lock'); return () => { const savedY = Number(body.dataset.scrollLockY || scrollY); body.classList.remove('scroll-lock'); body.style.top = ''; delete body.dataset.scrollLockY; window.requestAnimationFrame(() => window.scrollTo(0, Number.isFinite(savedY) ? savedY : 0)); }; }, [mobileOpen]);
  const showMissionTab = user && hasRole('assistant_zone_leader'); const showAdminTab = user && hasRole('president');
  return (<><nav className={`sticky top-0 z-40 pt-safe transition-all duration-300 ${scrolled ? 'bg-background-100/95 backdrop-blur-md shadow-sm border-b border-background-200/60 max-md:rounded-b-2xl max-md:shadow-card max-md:bg-background-100/75 max-md:backdrop-blur-lg max-md:border-b-0' : 'bg-transparent'}`}><div className="max-w-6xl mx-auto px-4 md:px-6"><div className={`relative flex justify-center items-center py-2 md:py-3 transition-all duration-300 ${scrolled ? 'max-md:py-1.5' : ''}`}><Link to="/" className="flex items-center gap-2 group cursor-pointer max-w-[calc(100%-64px)] md:max-w-none"><div className="w-8 h-8 rounded-lg bg-primary-100 max-md:bg-gradient-to-br max-md:from-primary-400 max-md:to-accent-400 flex items-center justify-center group-hover:bg-primary-200 transition-colors flex-shrink-0"><i className="ri-cross-line text-primary-600 max-md:text-white text-lg"></i></div><span className="font-bold text-foreground-950 text-sm md:text-base leading-tight truncate max-md:whitespace-normal max-md:line-clamp-2 md:whitespace-nowrap">스스로 신앙하는 거침없는 강릉 학생회</span></Link><div className="hidden md:flex items-center gap-2 absolute left-0">{TOP_ITEMS.map(item => <Link key={item.path} to={item.path} className={`px-2.5 py-1.5 rounded-lg text-xs font-semibold transition-colors ${isActive(item.path) ? 'bg-primary-100 text-primary-700' : 'text-foreground-600 hover:bg-background-100'}`}>{item.label}</Link>)}</div><div className="hidden md:flex items-center gap-2 absolute right-0">{showMissionTab && <div ref={missionRef} className="relative"><button onClick={() => { setBibleOpen(false); setFaithOpen(false); setCommOpen(false); setMissionOpen(!missionOpen); setAdminOpen(false); setProfileOpen(false); }} className="flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-xs font-semibold text-foreground-600 hover:bg-background-100 transition-colors cursor-pointer"><i className="ri-briefcase-4-line text-primary-500"></i>사명 도구<i className="ri-arrow-down-s-line"></i></button><AnimatePresence>{missionOpen && <motion.div initial={{ opacity: 0, y: -4, scale: .96 }} animate={{ opacity: 1, y: 0, scale: 1 }} exit={{ opacity: 0, y: -4, scale: .96 }} className="absolute right-0 top-full mt-2 w-64 bg-background-100 rounded-xl shadow-lg border border-background-200 overflow-hidden z-50"><div className="p-1.5">{MISSION_SUBSECTIONS.flatMap(s => s.items).map(item => item.path ? <Link key={item.path} to={item.path} onClick={() => setMissionOpen(false)} className="flex items-center gap-2.5 px-3 py-2.5 rounded-lg text-sm text-foreground-700 hover:bg-primary-50 transition-colors">{item.label}</Link> : null)}</div></motion.div>}</AnimatePresence></div>}{showAdminTab && <div ref={adminRef} className="relative"><button onClick={() => { setMissionOpen(false); setAdminOpen(!adminOpen); }} className="flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-xs font-semibold text-foreground-600 hover:bg-background-100 transition-colors cursor-pointer"><i className="ri-shield-user-line text-secondary-500"></i>관리<i className="ri-arrow-down-s-line"></i></button><AnimatePresence>{adminOpen && <motion.div initial={{ opacity: 0, y: -4, scale: .96 }} animate={{ opacity: 1, y: 0, scale: 1 }} exit={{ opacity: 0, y: -4, scale: .96 }} className="absolute right-0 top-full mt-2 w-56 bg-background-100 rounded-xl shadow-lg border border-background-200 overflow-hidden z-50"><div className="p-1.5">{ADMIN_CATEGORY_ITEMS.filter(i => hasRole(i.minRole)).map(item => <Link key={item.label} to={item.path} onClick={() => setAdminOpen(false)} className="flex items-center gap-2.5 px-3 py-2.5 rounded-lg text-sm text-foreground-700 hover:bg-secondary-50 transition-colors">{item.label}</Link>)}</div></motion.div>}</AnimatePresence></div>}{user ? <div className="relative" ref={profileRef}><button onClick={() => setProfileOpen(!profileOpen)} className="flex items-center gap-2 px-3 py-1.5 rounded-full bg-primary-50 hover:bg-primary-100 transition-all"><span className="w-7 h-7 rounded-full bg-amber-200 flex items-center justify-center text-xs font-bold text-amber-700">{profile?.name?.charAt(0) || '?'}</span><span className="text-xs font-medium text-foreground-800 max-w-[80px] truncate">{profile?.name || '불러오는 중...'}</span></button><AnimatePresence>{profileOpen && <motion.div initial={{ opacity: 0, y: -4 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -4 }} className="absolute right-0 top-full mt-2 w-60 bg-background-100 rounded-xl shadow-lg border border-background-200 z-50"><div className="p-3"><p className="text-sm font-semibold">{profile?.name}</p><p className="text-xs text-foreground-500 mt-1">{profile?.role ? ROLE_LABELS[profile.role] : ''}</p>{profile?.club && <p className="text-xs text-primary-600 mt-1">{CLUB_LABELS[profile.club]}</p>}<div className="mt-3 flex flex-col gap-1">{PROFILE_ACTIVITY_ITEMS.map(item => <Link key={item.path} to={item.path} onClick={() => setProfileOpen(false)} className="px-3 py-2 rounded-lg text-sm hover:bg-background-100">{item.label}</Link>)}<button onClick={handleSignOut} className="px-3 py-2 rounded-lg text-left text-sm text-rose-600 hover:bg-rose-50">로그아웃</button></div></div></motion.div>}</AnimatePresence></div> : <Link to="/login" className="px-3 py-1.5 rounded-full bg-amber-500 text-white text-xs font-medium">로그인</Link>}</div><div className="flex md:hidden items-center gap-2 absolute right-0">{user && <button onClick={() => setNotificationsOpen(!notificationsOpen)} className="w-9 h-9 rounded-full flex items-center justify-center hover:bg-background-200 transition-colors cursor-pointer relative"><i className="ri-notification-3-line text-lg text-foreground-600"></i>{notificationCount > 0 && <span className="absolute -top-0.5 -right-0.5 min-w-[16px] h-4 px-1 rounded-full bg-gradient-to-br from-accent-500 to-primary-500 text-white text-[9px] font-bold flex items-center justify-center shadow-card">{notificationCount > 99 ? '99+' : notificationCount}</span>}</button>}<button onClick={() => setMobileOpen(!mobileOpen)} className="w-9 h-9 rounded-full flex items-center justify-center hover:bg-background-200 transition-colors cursor-pointer"><i className={`text-xl text-foreground-700 ${mobileOpen ? 'ri-close-line' : 'ri-menu-line'}`}></i></button></div></div></div>{mobileOpen && <div className="md:hidden border-t border-background-200 bg-background-100/95 backdrop-blur max-h-[calc(100dvh-60px)] overflow-y-auto"><div className="px-4 py-3 grid gap-1">{TOP_ITEMS.map(item => <Link key={item.path} to={item.path} className="px-3 py-3 rounded-xl text-sm font-semibold text-foreground-700">{item.label}</Link>)}{showMissionTab && <div className="border-t border-background-200 pt-2 mt-1">{MISSION_SUBSECTIONS.flatMap(s => s.items).map(item => item.path ? <Link key={item.path} to={item.path} className="block px-3 py-3 rounded-xl text-sm text-foreground-700">{item.label}</Link> : null)}</div>}</div></div>}</nav><MeetingIdeasModal open={meetingIdeasOpen} onClose={() => setMeetingIdeasOpen(false)} /><NotificationsModal open={notificationsOpen} onClose={() => setNotificationsOpen(false)} user={user} />{user && <NotificationToast user={user} />}</>);
}