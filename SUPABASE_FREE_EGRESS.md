# Supabase Free 요금제 Egress 운영 규칙

이 문서는 다른 작업/채팅에서도 사진·데이터 전송량 최적화 기준이 유지되도록 프로젝트 소스에 남겨두는 운영 규칙이다.

## 핵심 원칙

Supabase Free에서는 사진을 "저장 용량"만 줄이는 것으로 충분하지 않다. 실제로 중요한 것은 **한 번의 화면 열람에서 내려받는 바이트 수 × 열람 횟수 × 사용자/기기 수**다.

따라서 새 기능을 만들거나 기존 화면을 수정할 때 다음 원칙을 우선한다.

1. **원본 사진을 사용자 화면에서 직접 내려받지 않는다.**
2. 업로드 시 브라우저에서 표시용 이미지와 썸네일을 생성한다.
3. 목록/그리드에서는 썸네일만 사용한다.
4. 상세/라이트박스에서는 표시용 이미지(최대 1280px 수준)를 사용한다.
5. 확대/핀치줌 때문에 원본을 추가 요청하지 않는다. 현재 확대는 이미 내려받은 표시용 이미지를 재사용한다.
6. 사진 목록은 한 번에 전부 렌더링하지 않고 페이지 단위로 제한한다. 현재 추억창 기본 페이지 크기는 24장이다.
7. 홈 화면에서는 필요한 소수의 썸네일만 조회한다.
8. Supabase DB 조회에서 `select('*')`를 습관적으로 사용하지 않는다. 실제 사용하는 컬럼만 명시한다.
9. 브라우저 캐시/Service Worker는 보조 수단으로만 취급한다. 캐시가 항상 적중한다고 가정하여 egress를 계산하지 않는다.
10. 새 이미지 기능을 추가할 때 원본 URL이 목록 화면의 `<img>` `src`로 들어가지 않는지 확인한다.

## 현재 적용된 코드 패턴

### 이미지 업로드

`src/lib/imageResize.ts`의 클라이언트 리사이즈를 사용한다.

- 표시용: 최대 1280px 수준
- 썸네일: 최대 480px 수준
- JPEG 품질을 낮춰 전송량을 줄인다.
- 업로드 실패 시 방금 생성/업로드한 파일을 정리하는 코드를 유지한다.
- 브라우저가 특정 소형 JPEG를 디코딩하지 못하는 기존 호환성 문제 때문에, 2MB 이하 JPEG 원본 fallback을 제거하지 않는다.

### 추억창

`src/pages/memoryBoard/page.tsx`

- 목록 이미지는 `thumb_url` 우선
- 데스크톱/모바일 모두 지연 로딩 사용
- 기본 표시 페이지 크기 24장
- DB 조회 시 필요한 컬럼만 명시
- 라이트박스에는 표시용 URL을 전달하고 썸네일은 필름스트립에 사용

### 동아리 상세

`src/pages/clubs/detail/page.tsx`

- 사진 그리드에는 `thumbUrl || url` 사용
- 표시용 사진은 라이트박스에서 사용
- 동아리 상세 데이터 조회는 `content`만 조회
- QnA 조회/등록은 실제 사용하는 컬럼만 조회

### 라이트박스

`src/components/feature/PhotoLightbox.tsx`

- 확대하더라도 원본 URL을 추가로 요청하지 않는다.
- 필름스트립은 썸네일 URL을 우선 사용한다.

## 새 기능 작업 전 체크리스트

- [ ] 목록 화면이 원본 사진 URL을 요청하지 않는가?
- [ ] 썸네일 URL이 있으면 반드시 썸네일을 우선 사용하는가?
- [ ] 상세 화면의 표시용 이미지 크기가 과도하지 않은가?
- [ ] 확대 동작이 원본 다운로드를 새로 발생시키지 않는가?
- [ ] 사진 목록에 페이지/개수 제한이 있는가?
- [ ] DB에서 `select('*')`를 사용하고 있지 않은가?
- [ ] 모바일에서 화면 밖 사진까지 불필요하게 로드하지 않는가?
- [ ] 업로드 중 실패하면 이미 올라간 Storage 파일을 정리하는가?
- [ ] Service Worker 캐시 적중을 egress 절감의 유일한 근거로 삼고 있지 않은가?

## 하지 말아야 할 것

- Supabase Storage 원본 사진을 목록/홈/카드에서 직접 사용하지 않는다.
- "사진이 작으니 5GB를 넘지 않는다"고 가정하지 않는다.
- 캐시가 있으므로 모든 사용자의 egress가 0에 가깝다고 가정하지 않는다.
- 실제 Supabase 사용량을 확인할 수 없는 상태에서 앱이 정확한 월간 quota를 자동으로 판단한다고 구현하지 않는다.
- 확인하지 않은 Supabase 테이블/컬럼을 추측하여 사용하지 않는다.

## 장기 구조

현재 구조는 **Supabase만 사용하면서 브라우저에서 이미지 크기를 줄이고, 썸네일 우선 표시와 목록 제한으로 egress를 낮추는 방식**이다.

향후 사진 사용량이 커져도 Supabase Free egress를 구조적으로 분리해야 한다면 사진 저장/전달 계층을 Cloudflare R2 등 별도 객체 저장소로 이전하는 방안을 검토할 수 있다. 이 경우에도 현재 DB/권한 구조를 임의로 바꾸지 말고 별도 설계와 검증 후 진행한다.

## 중요한 보장 범위

이 규칙은 egress를 크게 줄이는 구조이지 특정 월간 사용량을 수학적으로 보장하는 quota 보증 장치가 아니다. 실제 사용량은 사용자 수, 사진 열람 빈도, 기기별 캐시 상태, 사진 수, 네트워크 재요청 등에 따라 달라진다.
